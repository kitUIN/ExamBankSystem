<Page
    x:Class="ExamBankSystem.Views.UserView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ex="using:ExamBankSystem.Extensions"
    xmlns:local="using:ExamBankSystem.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Padding="10" Spacing="10">
            <Button Content="{ex:Locale Key=ChangePassword}" />
            <Button Command="{x:Bind ViewModel.LogoutCommand}" Content="{ex:Locale Key=Logout}" />
        </StackPanel>
    </Grid>
</Page>
